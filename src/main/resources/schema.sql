# CREATE TABLE inventory
    # (
          #     id       INT    NOT NULL AUTO_INCREMENT,
          #     scu_code varchar(45) NOT NULL,
    #     stock    INTEGER NOT NULL,
    #     PRIMARY KEY (id)
    # );